<% @arts.each do |art| %>
  <% art.images.each do |image| %>
    <%# image_tag image.variant(resize: "500x500").processed.service_url %>
  <% end %>
<% end %>

<%# image_tag @arts[103].images[0].variant(resize: "500x500").processed.service_url %>

<%= react_component("ArtsIndexPage", { arts: @arts, image: @image }) %>
