<%# @items.each do |item| %>
  <p><%# link_to "#{item.title}", item_path(item) %></p>
  <p><%# item.subtitle %></p>
  <p><%# item.description %></p>
  <p><%# number_to_currency(item.price) %></p>
<%# end %>

<%= react_component("ItemsIndexPage", { items: @items }) %>